# REQUIREMENT_OPTIMIZE_THS_INDUSTRY_HISTORY_LATENCY_20260615

## 1. 背景
- SW行业页切换到 `THS行业` 后，点击左侧板块时，中栏历史图表加载存在明显等待。
- 现有链路中，图表数据来自 `GET /api/industry-universe/history/`，THS分支优先调用上游 `ths_daily`，并在空结果场景回退到成分股中位数重建。
- 当前 THS history 接口缓存 TTL 仅 120 秒，导致用户在同一会话内重复切换时容易再次命中上游慢调用。

## 2. 目标
- 在不改变现有接口结构与前端布局的前提下，降低 THS 行业图表切换时延、减少重复等待。
- 避免快速连续点击时旧请求覆盖新请求导致的“看起来更慢”的体验。

## 3. 服务归属
- 前端：`smartinvestor_fe/src/views/SwIndustryView.vue`
- 后端：`smartinvestor_be/api/views.py`

## 4. 最小优化方案
1. 后端缓存优化（无契约变更）
   - 仅对 `industry_type=ths` 的 history 响应延长缓存 TTL（从 120 秒提升到 600 秒）。
   - 覆盖两条 THS 分支：
     - `ths_daily` 正常返回路径
     - `member_median` 回退路径

2. 前端请求治理（无契约变更）
   - `fetchIndustryHistory` 增加请求取消机制：
     - 新请求发起前取消前一个未完成请求。
     - 仅最后一次请求可回填页面状态。
   - 增加页面会话级 history 内存缓存（key: `industry_type + industry_key + metric + period`，TTL 5 分钟）：
     - 命中缓存时优先秒级回填图表，再执行后台请求刷新。
     - 保障快速来回切换同一板块时的响应速度。

3. 轻量交互优化
   - 对重复点击同一行业（类型与 key 都未变化）直接忽略，避免无意义重拉。

## 5. API契约影响
- 无新增参数、无字段变更、无返回结构变化。
- 前后端兼容性保持不变。

## 6. 验收标准
- THS模式下，用户在同一会话内重复切换已访问板块时，中栏图表可显著更快回显（缓存命中）。
- 快速连续点击多个板块时，最终图表与最后一次点击一致，不出现旧请求覆盖新请求。
- 后端日志无新增错误，现有 SW/行业变体/基本信息行业行为不受影响。

## 7. 风险与回退
- 风险：缓存 TTL 增加会带来短时间数据非实时性（最多 10 分钟）。
- 风险控制：仅作用于 THS history，且可通过缩短 TTL 快速回调。
- 回退：
  - 后端将 THS history TTL 改回 120 秒。
  - 前端移除请求取消与内存缓存逻辑。
