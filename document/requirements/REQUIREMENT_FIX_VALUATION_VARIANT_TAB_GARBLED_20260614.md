# REQUIREMENT_FIX_VALUATION_VARIANT_TAB_GARBLED_20260614

## 背景
- 在“估值一览 -> 多行业变体表格”的变体 Tab 中，最后一个默认变体的文本显示为乱码。

## 归属服务
- 前端服务：smartinvestor_fe
- 目标文件：smartinvestor_fe/src/components/StockValuationQuickView.vue

## 目标
- 修复最后一个默认变体 Tab 文本乱码问题。
- 当默认变体标签为空、乱码或不可读时，统一显示为“默认估值”。

## 非目标
- 不调整后端接口字段结构。
- 不改动估值计算逻辑。
- 不改动其他 Tab 的正常文案。

## 实现约束
- 仅做最小范围前端修复。
- 保持现有交互与排序逻辑不变。
- 保持兼容：优先使用后端提供标签，只有异常时才回退“默认估值”。

## 验收标准
- 估值一览中“多行业变体表格”最后一个默认变体 Tab 不再乱码。
- 其他变体 Tab 文案保持原样。
- 页面无编译错误。

## 验证步骤
1. 打开任一有多行业变体的股票“估值一览”。
2. 展开“多行业变体表格”。
3. 检查最后一个默认变体 Tab 文案应为“默认估值”（不乱码）。
4. 执行前端构建/类型检查，确认无新增错误。
