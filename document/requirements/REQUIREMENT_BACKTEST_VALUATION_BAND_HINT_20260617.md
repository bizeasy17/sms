# 需求说明：回测页面补充“估值带宽”提示说明

## 背景
- 回测页面存在“估值带宽”输入项 `band_pct`。
- 该参数在页面上仅显示字段名，缺少解释，用户容易把它理解成固定折扣或绝对价差。

## 目标
- 在前台回测页面为“估值带宽”补充简短说明。
- 保持现有参数、接口和默认值不变。

## 建议说明口径
- 估值带宽表示：回测时允许当前价格相对估值中枢上下浮动的容忍区间。
- 带宽越大，筛选条件越宽松；带宽越小，筛选条件越严格。
- 当前页面输入值为小数，例如 `0.10` 表示 10%。

## 影响范围
- 仅修改 `smartinvestor_fe/src/views/BacktestExecuteView.vue` 的文案提示。
- 不修改后端计算逻辑。

## 验收标准
- 用户将鼠标悬停或查看提示时能理解 `band_pct` 的含义。
- 页面其他回测参数行为不受影响。
