# 需求说明：moneyflow_ths 字段与数据库字段 1:1 对齐

## 1. 背景
- 当前 THS 个股资金流入库模型 `StockThsMoneyflowDaily` 主要使用字段 `net_mf_amount`。
- 近期核验发现历史区间存在“有行但净流入字段为空”的情况，且当前库字段命名与 `moneyflow_ths` 原始字段并非完全同名对齐。
- 为降低口径歧义与排障成本，需要将数据库字段名与 `moneyflow_ths` 字段做 1:1 对齐。

## 2. 目标
1. 数据库表新增并维护与 `moneyflow_ths` 完全同名的关键字段，做到 1:1 映射落库。
2. 同步逻辑改为“同名字段直存”，保留原始 `raw_payload` 作为审计副本。
3. 回测与筛选逻辑优先读取同名字段，旧字段仅做兼容兜底（过渡期）。
4. 对指定历史区间回补后，验证资金流条件下回测具备可用命中样本。

## 3. 服务归属（待确认）
- 归属建议：`smartinvestor_be` 独立实现（模型、迁移、同步、回测读取）。

## 4. 范围
- 后端：
  - 模型字段调整（新增同名字段）
  - 迁移脚本
  - 同步命令/函数入库映射调整
  - 回测与资金流过滤读取口径切换
  - 历史区间回补与特征重建
- 前端：本期不涉及。

## 5. 非目标
- 不改 TuShare 上游接口。
- 不改页面交互。
- 不引入分钟级资金流。

## 6. 字段对齐方案（草案）

### 6.1 目标字段（与 moneyflow_ths 同名）
- `ts_code`
- `trade_date`
- `net_amount`
- `net_pct`
- `net_amount_rate`
- `net_mf_amount`
- `net_mf_rate`
- `buy_sm_amount`
- `sell_sm_amount`
- `buy_md_amount`
- `sell_md_amount`
- `buy_lg_amount`
- `sell_lg_amount`
- `buy_elg_amount`
- `sell_elg_amount`

说明：若上游在不同版本存在字段差异，以当前 `moneyflow_ths` 实际返回为准；缺失字段允许 null。

### 6.2 兼容策略
- 过渡期保留旧读取路径：
  - 优先读同名字段（例如 `net_amount`）。
  - 为空时再回退到旧字段（如现有 `net_mf_amount`）。
- 过渡期结束后再决定是否移除旧兜底。

## 7. 数据迁移与回补
1. 新增迁移：为 `StockThsMoneyflowDaily` 增加同名字段。
2. 同步逻辑更新后，按区间回补：`2025-06-15` 至 `2025-12-31`（先验证区间）。
3. 重建资金流特征表（5/10/15/30/60 窗口）。
4. 对比回补前后非空率与回测命中数。

## 8. 回测口径调整
- 回测资金流过滤默认读取：`net_amount`（同名字段）。
- 保留兜底：`net_mf_amount`。
- 过滤规则暂保持不变：最近 N 日累计净流入 > 0（后续可单独优化）。

## 9. 验收标准
1. 指定历史区间内同名字段非空率显著提升（`net_amount` 非空 > 0）。
2. `raw_payload` 与同名字段可抽样一致。
3. 回测开启资金流过滤后，不再因字段全空导致必然 0 交易（至少出现可解释命中样本）。
4. `manage.py check` 通过，迁移可重复执行。

## 10. 风险与回退
- 风险：上游字段返回不稳定、历史区间仍为空值。
- 回退：
  - 读取路径切回旧字段优先。
  - 同步流程保留旧字段写入，不影响主链路。

## 11. 待确认项
1. 是否确认由 `smartinvestor_be` 独立实现。
2. 是否确认回测读取优先字段为 `net_amount`。
3. 历史回补首批区间是否确认仅 `2025-06-15`~`2025-12-31`。
4. 过滤规则本期是否保持“累计净流入 > 0”不变。

## 12. 备注
- 本文档确认后再进入代码实施。
