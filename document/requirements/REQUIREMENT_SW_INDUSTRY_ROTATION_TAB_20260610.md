# SW 行业页面新增“行业轮动”Tab需求（草案）

## 1. 背景
当前 SW 行业页面已提供 sw-history（行业指数、PE/PB、基础面展示）和行业收藏能力。现需在不破坏现有布局前提下，新增“行业轮动”能力，输出 TopN 行业候选。

## 2. 页面范围与布局约束
- 目标页面：`smartinvestor_fe/src/views/SwIndustryView.vue`
- Tab 位置：新增 Tab 放在“全部行业”之后、“收藏行业”之前。
- 布局约束：
  - 不调整现有主区块结构（行业列表、历史图、现有筛选）
  - 新功能在新 Tab 内独立展示
  - 默认不影响原“全部行业/收藏行业”交互与性能

## 3. 功能目标
- 新增 Tab：`行业轮动`
- 输出内容：
  - TopN 行业候选列表（默认 N=10，可配置）
  - 每个候选的轮动综合分、风格（regime）、主要解释因子
  - 快照时间与计算版本

## 4. 数据与口径
- 数据输入：
  - sw-history：行业指数与估值（PE/PB）
  - 既有行业 regime 映射（含 suggested_v1 覆盖）
- 统一口径：
  - 传统估值与预测估值使用同一 regime 映射结果
  - 行业轮动亦复用同一 regime 结果，不再维护第三套分类

## 5. 轮动评分（V1）
- 建议评分项：
  - 估值因子：PE/PB 分位（越低越优）
  - 动量因子：1M/3M 相对强弱
  - 风险因子：波动/回撤惩罚
  - 风格匹配：当前市场偏好与行业 regime 匹配度
- 输出字段（最小集）：
  - `industry_code`
  - `industry_name`
  - `regime`
  - `rotation_score`
  - `score_breakdown`（valuation/momentum/risk/style）
  - `asof_date`

## 6. 计算模式选项（你关心的核心）
### 方案A：请求时实时计算
- 优点：
  - 数据最新
  - 无离线任务依赖
- 缺点：
  - 请求耗时高
  - 并发压力大
  - 前端体验可能抖动

### 方案B：定期预计算，前端读预存结果
- 优点：
  - 页面响应快
  - 结果稳定可复现
  - 易做版本管理和回溯
- 缺点：
  - 新鲜度受任务周期限制

### 方案C（推荐）：预计算为主 + 手动刷新补充
- 默认：前端读取预存快照（B）
- 可选：提供“立即重算”按钮（异步触发）
- 优点：
  - 兼顾性能与时效
  - 风险最低，适合先落地

## 7. 推荐实施方案（V1）
- 采用方案C：
  - 后端定期任务生成轮动快照（建议日频收盘后）
  - 新增查询接口返回最新快照与TopN
  - 前端 Tab 默认读最新快照，支持手动刷新

## 8. 接口建议（草案）
- `GET /api/sw-industry/rotation/latest/?top_n=10`
  - 返回：最新快照元信息 + TopN 列表
- `POST /api/sw-industry/rotation/recompute/`（可选）
  - 触发异步重算，返回任务ID

## 9. 验收标准
- 新 Tab 出现在“全部行业”和“收藏行业”之间
- 原有两个 Tab 功能与布局无回归
- 行业轮动 Tab 可展示 TopN 与解释字段
- 接口不可用时，Tab 仅显示清晰空态/错误，不影响原页面

## 10. 风险与回滚
- 风险：因子权重不稳导致候选波动过大
- 控制：记录 `scoring_version` + `asof_date`
- 回滚：关闭轮动 Tab 数据源，保留页面入口或临时隐藏入口

## 11. 待你确认
1. 服务归属是否确认由 `smartinvestor_be` 实现后端计算与接口？
2. 计算模式是否确认采用“方案C（预计算为主 + 手动刷新）”？
3. TopN 默认值是否确认 `N=10`？

## 12. TopN后验表现评估（新增）
- 目标：评估“某次行业轮动 TopN”在后续一段时间内的市场表现。
- run 快照：每次有效 TopN 生成时写入 run（run_id/asof_date/top_n/scoring_version/候选明细）。
- 观察窗口：默认支持 5/20/60 交易日，可扩展。
- 指标：
  - TopN 等权收益
  - 基准收益（SW L3 等权）
  - 超额收益（TopN - 基准）
  - 命中率（收益为正的行业占比）
- 产出：
  - run 汇总列表（每期表现）
  - run 详情（每个行业在各窗口的收益）

## 13. run 生成规则（新增）
- 规则：每次生成 TopN 都创建新 run，不做去重。
- run 标识：每次写入新的 `run_id`，用于后验表现追踪与回溯。
- 接口约定：重算接口返回 `run_created=true` 与对应 `run_id`。
- 说明：即使 TopN 与上一期完全一致，也保留为独立 run，便于按时间维度比较后续表现。

## 14. runid 发展图表展示（新增）
- 目标：在前端按 `run_id` 查看该次 TopN 的后验表现，并用图表展示发展情况。
- 最小代价方案：
  - 页面不新增路由，不拆新页面；在“行业轮动”Tab 内增加一个“查看run发展”入口。
  - 打开弹窗后先展示 run 列表（`run_id/created_at/asof_date`），点击某个 run 后加载详情。
  - 图表仅展示窗口维度对比：`TopN收益`、`基准收益`、`超额收益`、`命中率`。
- 接口复用：
  - 列表：`GET /api/industry-universe/rotation/runs/?limit=20`
  - 详情：`GET /api/industry-universe/rotation/runs/{run_id}/?windows=5,20,60`
- 图表形态（V1）：
  - ECharts 柱线混合图：
    - X轴：窗口（5/20/60）
    - 左Y轴：收益率（TopN/基准/超额，百分比）
    - 右Y轴：命中率（0-100%）
  - 若数据为空，展示空态，不影响轮动主列表。
- 交互约束：
  - 默认选中最近一次 run。
  - 与现有行业单击逻辑解耦，不改变当前行业列表行为。
- 验收补充：
  - 能在前端看到 run 列表并选择指定 run。
  - 选择 run 后能看到 5/20/60 窗口图表，字段与后端返回一致。
  - 后端无数据或接口异常时，弹窗显示明确提示且主页面可正常使用。

## 15. runid 真实表现定期评估（新增）
- 目标：将 run 的后验表现从“按需点击计算”升级为“定期自动计算并落库（文件）”，保障列表秒开且指标持续更新。

### 15.1 方案（最小代价）
- 保持现有存储：继续使用 `output/sw_rotation/sw_industry_rotation_runs.json`。
- TopN 生成频率：月频（建议每月一次，或手动触发）。
- TopN 表现评估频率：日频（交易日每日刷新），将每个 run 的表现按日写入预计算结果。
- 复用现有计算逻辑并扩展日序列，不新增第二套指标口径。

### 15.2 调度与覆盖范围
- 调度频率（建议）：
  - 月频任务：生成/重算 TopN（run 创建）。
  - 日频任务：刷新 run 表现序列与窗口汇总（5/20/60）。
- 默认窗口：`5,20,60`（可通过命令参数覆盖）。
- 评估对象：
  - 默认评估全部存量 run；
  - 支持参数限制最近 N 条 run（例如最近 200 条），用于快速补跑。

### 15.3 后端实现建议
- 新增管理命令（建议名）：
  - `python manage.py refresh_sw_rotation_run_evaluation_daily --windows 5,20,60 --limit 200`
- 命令行为：
  - 加载 runs 文件；
  - 对目标 run 逐条计算“按日表现序列 + 窗口汇总”；
  - 写回：
    - `evaluation_daily`：按日期保存当日 TopN/基准/超额/命中率；
    - `last_evaluation`：保留 5/20/60 窗口汇总，用于列表快速展示；
  - 输出统计：总数、成功数、失败数、耗时。
- 异常策略：单条 run 失败不中断全任务，记录错误并继续。

### 15.4 接口与前端协同
- 列表接口继续只读 `last_evaluation`，不做现场重算（已实现）。
- 详情接口改为优先返回预计算 `evaluation_daily`，仅在缺失时回退即时计算。
- 前端图表优先展示 `evaluation_daily`（按天曲线），窗口 5/20/60 作为摘要指标展示。

### 15.5 run 删除能力（新增）
- 目标：在“TopN表现”弹窗左侧 run 列表，为每个 run 提供删除按钮。
- 后端接口建议：
  - `DELETE /api/industry-universe/rotation/runs/{run_id}/`
  - 行为：从 runs 文件中删除指定 run，返回剩余条数。
- 前端交互建议：
  - 每个 run 行右侧展示删除按钮；
  - 删除前二次确认；
  - 删除后自动刷新列表并回填选中项（优先选下一条）。

### 15.6 验收标准
- 定时任务执行后，runs 列表大多数 run 存在 `last_evaluation`。
- 前端“查看topN表现”弹窗默认加载预计算结果，按天曲线可直接展示。
- 至少抽样 3 个 run，验证 `evaluation_daily` 与窗口摘要口径一致（允许小数四舍五入差异）。
- 左侧 run 删除后列表与图表状态正确刷新。

### 15.7 回滚与风险
- 风险：批量评估调用外部数据源，可能耗时或受限流影响。
- 控制：支持 `--limit` 分批补跑；失败 run 记录后重试。
- 回滚：若任务异常，可临时停调度，详情接口回退到按需重算；删除接口可临时下线。
