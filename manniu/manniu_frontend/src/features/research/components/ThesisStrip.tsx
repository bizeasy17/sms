export function ThesisStrip({ score = null, buyCandidate = null, riskLevel = null }: { score?: number | null; buyCandidate?: boolean | null; riskLevel?: string | null }) { const riskLabels: Record<string, string> = { HIGH: '高', MEDIUM: '中', LOW: '低' }; const displayRisk = riskLevel ? riskLabels[riskLevel] ?? riskLevel : '暂无'; const verdict = buyCandidate === true ? '买入候选' : buyCandidate === false ? '中性持有' : '暂无研究结论'; return <section className="thesis-strip"><div className="thesis-verdict"><span className="verdict-dot" /><div><p className="kicker">CURRENT VIEW</p><h2>{verdict}</h2></div></div><div className="thesis-copy"><strong>基本面改善正在兑现，但市场预期已反映部分增长。</strong><p>当前估值结果来自已发布的传统估值快照。</p></div><div className="confidence"><span>估值分数</span><strong>{typeof score === 'number' ? Math.round(score) : '暂无'}</strong>{typeof score === 'number' && <small>/ 100</small>}<span className="risk-level"><span>风险级别</span><strong>{displayRisk}</strong></span></div></section> }