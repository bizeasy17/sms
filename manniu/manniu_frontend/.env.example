# Browser-facing API prefix. Keep this relative when using the Vite dev proxy.
VITE_API_BASE_URL=/api/v1

# Django development server origin used by vite.config.ts.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
